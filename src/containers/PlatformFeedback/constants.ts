/**
 * Claim `subject` URI for platform feedback (rating + endorsement flows).
 * `PlatformFeedbackForm` sends this on `POST /api/claims` so the graph ties the claim to LinkedTrust.
 */
export const PLATFORM_FEEDBACK_SUBJECT = 'https://linkedtrust.us/'

export const LINKEDTRUST_DISPLAY_URL = 'linkedtrust.us'
